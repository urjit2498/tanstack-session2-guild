# Copy to .env.local for local overrides (do not commit .env.local).
#
# Production: set your public API origin in the Netlify dashboard under
# Site configuration → Environment variables. Use the Vite prefix for
# client-exposed vars (see Vite env docs). Rebuild after changing.
#
# Example value shape: https://your-json-server.example.com
